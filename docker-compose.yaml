version: '3.8'

services:
  todo-api:
    build:
      context: .
      dockerfile: dockerfile
    ports: 
      - "8080:8080"
    environment:
      - DATABASE_URL=postgres://postgres:FooBar123!@db:5432/tododb?sslmode=disable
    depends_on:
      - db

  db:
    image: postgres:13
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=FooBar123!
      - POSTGRES_DB=tododb
    ports:
      - "5432:5432"